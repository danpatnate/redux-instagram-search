import React from 'react';

const NotFound = () => (
  <div className="partial-wrapper">
    <h1 className="page-header">404 page not found</h1>
    <p>We are sorry but that page doesn't exist.</p>
  </div>
);

NotFound.propTypes = {};

export default NotFound;
